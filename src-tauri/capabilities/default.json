{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的基础能力。dsh 的 web 页面运行在 http://127.0.0.1 远程源上，不受此能力影响，拿不到任何 Tauri IPC。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
