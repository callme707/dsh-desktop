{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "只授予本地 chrome 顶栏基础能力。content WebView（包括 http://127.0.0.1:3080 的 dsh 页面）不匹配此能力，拿不到任何 Tauri IPC。",
  "webviews": ["chrome"],
  "permissions": ["core:default"]
}
